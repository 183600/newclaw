services:
  - type: web
    name: iflow
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: IFLOW_STATE_DIR
        value: /data/.iflow
      - key: IFLOW_WORKSPACE_DIR
        value: /data/workspace
      - key: IFLOW_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: iflow-data
      mountPath: /data
      sizeGB: 1
